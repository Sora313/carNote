"use client";

import { Pencil, Plus, Search, Trash2, X } from "lucide-react";
import { useEffect, useState } from "react";
import { carsApi, expensesApi, type Car, type Expense } from "@/lib/api";
import { ProtectedShell } from "@/components/protected-shell";

const emptyExpense = { name: "", type: "Mantenimiento", amount: "", frequency: "Mensual", carId: "" };
export default function GastosPage() {
  const [expenses, setExpenses] = useState<Expense[]>([]); const [cars, setCars] = useState<Car[]>([]); const [query, setQuery] = useState(""); const [form, setForm] = useState(emptyExpense); const [editing, setEditing] = useState<Expense | null>(null); const [open, setOpen] = useState(false); const [error, setError] = useState("");
  async function loadData() { try { const [expenseList, carList] = await Promise.all([expensesApi.list(), carsApi.list()]); setExpenses(expenseList); setCars(carList); if (!form.carId && carList[0]) setForm((current) => ({ ...current, carId: carList[0]._id })); } catch (cause) { setError(cause instanceof Error ? cause.message : "No se pudieron cargar los gastos"); } }
  useEffect(() => { let active = true; Promise.all([expensesApi.list(), carsApi.list()]).then(([expenseList, carList]) => { if (!active) return; setExpenses(expenseList); setCars(carList); }).catch((cause: unknown) => { if (active) setError(cause instanceof Error ? cause.message : "No se pudieron cargar los gastos"); }); return () => { active = false; }; }, []);
  const filtered = expenses.filter((expense) => `${expense.name} ${expense.type} ${typeof expense.carId === "object" ? expense.carId.name : expense.carId}`.toLowerCase().includes(query.toLowerCase()));
  function startEdit(expense: Expense) { const carId = typeof expense.carId === "object" ? expense.carId._id : expense.carId; setEditing(expense); setForm({ name: expense.name, type: expense.type, amount: String(expense.amount), frequency: expense.frequency, carId }); setOpen(true); }
  async function submit(event: React.FormEvent) { event.preventDefault(); try { const payload = { ...form, amount: Number(form.amount) }; if (editing) await expensesApi.update(editing._id, payload); else await expensesApi.create(payload); setOpen(false); setEditing(null); setForm({ ...emptyExpense, carId: cars[0]?._id ?? "" }); await loadData(); } catch (cause) { setError(cause instanceof Error ? cause.message : "No se pudo guardar el gasto"); } }
  async function remove(id: string) { if (!window.confirm("¿Eliminar este gasto?")) return; try { await expensesApi.remove(id); setExpenses(expenses.filter((expense) => expense._id !== id)); } catch (cause) { setError(cause instanceof Error ? cause.message : "No se pudo eliminar el gasto"); } }
  return <ProtectedShell title="Gastos"><div className="page-content"><div className="page-heading"><div><p className="eyebrow">CONTROL FINANCIERO</p><h1>Gastos</h1><p className="subtitle">Registra y administra los gastos de todos tus vehículos.</p></div><button className="primary-button" onClick={() => { setEditing(null); setForm({ ...emptyExpense, carId: cars[0]?._id ?? "" }); setOpen(true); }}><Plus size={18} /> Nuevo gasto</button></div><section className="panel crud-panel"><div className="crud-toolbar"><div><h2>Registro de gastos</h2><p>{expenses.length} gastos registrados</p></div><div className="search-box"><Search size={16} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Buscar gasto..." /></div></div>{error && <p className="error-message">{error}</p>}<div className="table-wrap"><table><thead><tr><th>CONCEPTO</th><th>TIPO</th><th>MONTO</th><th>PERIODICIDAD</th><th>AUTO</th><th>ESTADO</th><th>ACCIONES</th></tr></thead><tbody>{filtered.map((expense) => { const car = typeof expense.carId === "object" ? expense.carId.name : cars.find((item) => item._id === expense.carId)?.name ?? "Sin auto"; return <tr key={expense._id}><td><strong>{expense.name}</strong></td><td><span className="type-badge">{expense.type}</span></td><td className="amount">${expense.amount.toFixed(2)}</td><td>{expense.frequency}</td><td>{car}</td><td><span className={`status ${expense.status === "Pagado" ? "paid" : "pending"}`}><i />{expense.status}</span></td><td><div className="row-actions"><button className="action-button" onClick={() => startEdit(expense)} aria-label="Editar"><Pencil size={15} /></button><button className="action-button danger" onClick={() => void remove(expense._id)} aria-label="Eliminar"><Trash2 size={15} /></button></div></td></tr>; })}</tbody></table></div>{!filtered.length && <div className="empty-crud">Aún no hay gastos para mostrar.</div>}</section></div>{open && <div className="modal-backdrop" onMouseDown={() => setOpen(false)}><div className="modal" onMouseDown={(event) => event.stopPropagation()}><div className="modal-header"><div><p className="eyebrow">CRUD DE GASTOS</p><h2>{editing ? "Editar gasto" : "Nuevo gasto"}</h2></div><button className="close-button" onClick={() => setOpen(false)}><X size={19} /></button></div><form onSubmit={submit}><label>Nombre del gasto<input value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} placeholder="Cambio de aceite" required /></label><div className="form-row"><label>Tipo<select value={form.type} onChange={(event) => setForm({ ...form, type: event.target.value })}><option>Mantenimiento</option><option>Seguro</option><option>Operativo</option><option>Impuestos</option><option>Cuidado</option></select></label><label>Monto<input type="number" min="0" step="0.01" value={form.amount} onChange={(event) => setForm({ ...form, amount: event.target.value })} placeholder="0.00" required /></label></div><div className="form-row"><label>Periodicidad<select value={form.frequency} onChange={(event) => setForm({ ...form, frequency: event.target.value })}><option>Una vez</option><option>Semanal</option><option>Mensual</option><option>Anual</option><option>Cada 6 meses</option></select></label><label>Auto<select value={form.carId} onChange={(event) => setForm({ ...form, carId: event.target.value })} required><option value="">Selecciona un auto</option>{cars.map((car) => <option key={car._id} value={car._id}>{car.name} · {car.plate}</option>)}</select></label></div><div className="modal-actions"><button type="button" className="secondary-button" onClick={() => setOpen(false)}>Cancelar</button><button className="primary-button" disabled={!cars.length}>Guardar gasto</button></div></form></div></div>}</ProtectedShell>;
}
